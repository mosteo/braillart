name = "sand_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
sand = "*"

[[pins]]
sand = { path = ".." }

[build-profiles]
sand       = "validation"
sand_tests = "validation"
